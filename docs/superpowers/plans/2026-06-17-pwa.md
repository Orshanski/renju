# PWA-обвязка (rj-puo) — План реализации

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Сделать фронт устанавливаемым PWA (иконка на домашний экран, standalone) + аккуратная реакция на потерю сети (баннер «Нет связи», авто-reconnect), без офлайн-игры.

**Architecture:** Офлайн обрабатывается в React (хук `useOnlineStatus` + баннер в `Shell` + reconnect в `useGame` по событию `online`). Service worker — минимальный (`vite-plugin-pwa`): installability + precache оболочки, `navigateFallback` на `index.html`, `/api/*` в denylist. CSP-совместимая регистрация (ручной `registerPwa`, не inline).

**Tech Stack:** React 19, TypeScript, Vite 6, Vitest, vite-plugin-pwa (Workbox); FastAPI (StaticFiles + SPA-fallback).

## Global Constraints

- Команды фронта из `frontend/`; бэка — из `backend/` через `uv run …`
- Фронт-гейт: `npx tsc --noEmit` (0 ошибок) + `npx vitest run` (все зелёные); бэк-гейт: `uv run ruff check app tests` + `uv run pytest -q`
- CSP проекта: `script-src 'self' https://static.cloudflareinsights.com` — НЕ срезать; регистрация SW НЕ inline (`injectRegister: null` + module `registerPwa`)
- `import { type X }` для типов (isolatedModules)
- `tsconfig.json` `compilerOptions.types` += `"vite-plugin-pwa/client"` (иначе `tsc` красный на `virtual:pwa-register`)
- Офлайн-игры НЕТ; SW не кеширует `/api/*` и SSE
- Иконки — из `~/Downloads/image.png` (1254×1254), результат коммитим в `frontend/public/`

---

### Task 1: Хук статуса соединения `useOnlineStatus`

**Files:**
- Create: `frontend/src/hooks/useOnlineStatus.ts`
- Test: `frontend/src/hooks/useOnlineStatus.test.ts`

**Interfaces:**
- Produces: `useOnlineStatus(): boolean` — true когда `navigator.onLine`, реагирует на события `online`/`offline`.

- [ ] **Step 1: Тест**

```ts
// frontend/src/hooks/useOnlineStatus.test.ts
import { renderHook, act } from "@testing-library/react";
import { it, expect, vi, afterEach } from "vitest";
import { useOnlineStatus } from "./useOnlineStatus";

// общий setup.ts не делает restoreAllMocks → снимаем spy на navigator.onLine сами,
// чтобы мок не протёк в другие файлы
afterEach(() => vi.restoreAllMocks());

it("стартует со значением navigator.onLine", () => {
  vi.spyOn(navigator, "onLine", "get").mockReturnValue(true);
  const { result } = renderHook(() => useOnlineStatus());
  expect(result.current).toBe(true);
});

it("offline-событие → false, online-событие → true", () => {
  vi.spyOn(navigator, "onLine", "get").mockReturnValue(true);
  const { result } = renderHook(() => useOnlineStatus());
  act(() => { window.dispatchEvent(new Event("offline")); });
  expect(result.current).toBe(false);
  act(() => { window.dispatchEvent(new Event("online")); });
  expect(result.current).toBe(true);
});
```

- [ ] **Step 2: Запустить — FAIL**

Run: `cd frontend && npx vitest run src/hooks/useOnlineStatus.test.ts`
Expected: FAIL (модуль не существует)

- [ ] **Step 3: Реализация**

```ts
// frontend/src/hooks/useOnlineStatus.ts
import { useEffect, useState } from "react";

export function useOnlineStatus(): boolean {
  const [online, setOnline] = useState(navigator.onLine);
  useEffect(() => {
    const on = () => setOnline(true);
    const off = () => setOnline(false);
    globalThis.addEventListener("online", on);
    globalThis.addEventListener("offline", off);
    return () => {
      globalThis.removeEventListener("online", on);
      globalThis.removeEventListener("offline", off);
    };
  }, []);
  return online;
}
```

- [ ] **Step 4: PASS + tsc**

Run: `cd frontend && npx vitest run src/hooks/useOnlineStatus.test.ts && npx tsc --noEmit`
Expected: PASS, 0 ошибок

- [ ] **Step 5: Commit**

```bash
git add frontend/src/hooks/useOnlineStatus.ts frontend/src/hooks/useOnlineStatus.test.ts
git commit -m "feat(rj-puo): useOnlineStatus — статус соединения"
```

---

### Task 2: Баннер «Нет связи» в `Shell`

**Files:**
- Modify: `frontend/src/components/Shell.tsx`
- Modify: `frontend/src/components/Shell.module.css`
- Modify: `frontend/src/components/Shell.test.tsx` (УЖЕ существует — 8 тестов, реальный `AuthProvider` + msw, helper `tree()`)

**Interfaces:**
- Consumes: `useOnlineStatus` (Task 1)

- [ ] **Step 1: Добавить 2 кейса в существующий Shell.test.tsx**

НЕ перезаписывать файл (там 8 рабочих тестов). Добавить два `it` в его стиле (helper `tree()`, реальный `AuthProvider`, `server.use(meOk)`; `act` дописать в импорт из `@testing-library/react`, если его там нет):

```tsx
it("онлайн — баннера «Нет связи» нет", async () => {
  server.use(http.get("/api/auth/me", () => HttpResponse.json({ id: 1, username: "alice", role: "user" })));
  tree();
  await waitFor(() => expect(screen.getByText("alice")).toBeInTheDocument());
  expect(screen.queryByText(/нет связи/i)).not.toBeInTheDocument();
});

it("офлайн — баннер «Нет связи» виден", async () => {
  server.use(http.get("/api/auth/me", () => HttpResponse.json({ id: 1, username: "alice", role: "user" })));
  tree();
  await waitFor(() => expect(screen.getByText("alice")).toBeInTheDocument());
  act(() => { window.dispatchEvent(new Event("offline")); });
  expect(screen.getByText(/нет связи/i)).toBeInTheDocument();
});
```

(`tree()`, `server`, `http`, `HttpResponse`, `screen`, `waitFor` — уже в файле; баннер от auth не зависит, но ждём `alice` для стабильности рендера, как соседние тесты.)

- [ ] **Step 2: FAIL**

Run: `cd frontend && npx vitest run src/components/Shell.test.tsx`
Expected: новый офлайн-кейс FAIL (баннера нет); существующие 8 — PASS

- [ ] **Step 3: Реализация — Shell.tsx**

В `frontend/src/components/Shell.tsx`: добавить импорт `import { useOnlineStatus } from "../hooks/useOnlineStatus";`, в теле `Shell()` — `const online = useOnlineStatus();`, и баннер сразу под `<header>…</header>` (перед `<main>`):

```tsx
{!online && (
  <div className={styles.offlineBar} role="status">
    Нет связи — проверьте соединение
  </div>
)}
```

- [ ] **Step 4: Реализация — Shell.module.css**

Добавить класс (`vermillion` и `fontSans` уже импортированы в `@value`-шапке `Shell.module.css` — дополнять импорт не нужно):

```css
.offlineBar {
  background: vermillion;
  color: #fff;
  font-family: fontSans;
  font-size: 13px;
  text-align: center;
  padding: 6px 12px;
}
```

- [ ] **Step 5: PASS + tsc**

Run: `cd frontend && npx vitest run src/components/Shell.test.tsx && npx tsc --noEmit`
Expected: PASS, 0 ошибок

- [ ] **Step 6: Commit**

```bash
git add frontend/src/components/Shell.tsx frontend/src/components/Shell.module.css frontend/src/components/Shell.test.tsx
git commit -m "feat(rj-puo): баннер «Нет связи» в Shell"
```

---

### Task 3: Авто-reconnect `useGame` по событию `online`

**Files:**
- Modify: `frontend/src/game/useGame.ts`
- Modify: `frontend/src/game/useGame.test.tsx`

**Interfaces:**
- Consumes: внутренний `connect(since)` и `esRef`/`aliveRef`/`viewRef` хука `useGame`

**Контекст:** в `useGame` основной `useEffect` (вызывает `enterGame` + начальный `getGame` + `connect`) — туда добавить подписку на `online`. При `online` форсировать `connect`, если хук жив и нет открытого `EventSource`.

- [ ] **Step 1: Тест (точный код)**

`FakeEventSource` (frontend/src/test/eventsource.ts) уже имеет `static instances[]`, `.last()`, `.fail()` (дёргает `onerror`), `close()` (ставит `readyState=2`). Ничего в харнес добавлять НЕ надо. Тест монтирует `useGame` через `renderHook` с **большим** `reconnectDelayMs` (чтобы таймерный reconnect из `onerror` не выстрелил и не создал второй инстанс сам — иначе тест ложно-зелёный):

Файл уже имеет `beforeEach` с `installFakeEventSource()`+`FakeEventSource.reset()`, helper `state()` и импорты `renderHook/act/waitFor`, `server/http/HttpResponse`. Дублировать не надо — только новый `it`:

```tsx
// добавить в frontend/src/game/useGame.test.tsx
it("событие online переподключает SSE после обрыва", async () => {
  server.use(http.get("/api/games/g1", () => HttpResponse.json(state())));
  // большой reconnectDelayMs: таймерный reconnect из onerror не успеет создать 2-й инстанс —
  // проверяем именно online-путь, а не таймер (иначе тест ложно-зелёный)
  const { unmount } = renderHook(() => useGame("g1", 100000));
  await waitFor(() => expect(FakeEventSource.instances).toHaveLength(1));
  act(() => FakeEventSource.last().fail());      // обрыв → onerror → es.close(), readyState=2
  act(() => { window.dispatchEvent(new Event("online")); });
  await waitFor(() => expect(FakeEventSource.instances).toHaveLength(2)); // online создал новый стрим
  unmount();
});
```

`useGame(gameId, reconnectDelayMs)` — второй параметр уже есть в сигнатуре. `state()` — существующий helper файла (валидный GameStateDTO).

- [ ] **Step 2: FAIL**

Run: `cd frontend && npx vitest run src/game/useGame.test.tsx`
Expected: новый тест FAIL (online не переподключает)

- [ ] **Step 3: Реализация**

В `frontend/src/game/useGame.ts`, в основном `useEffect` (где `enterGame`/`getGame`/`connect`), добавить подписку на `online` и снять её в cleanup:

```ts
const onOnline = () => {
  // поток закрыт/отсутствует → переподключиться. readyState сравниваем с литералом 2
  // (CLOSED) — как везде в useGame; FakeEventSource в тестах не имеет статической
  // константы EventSource.CLOSED, и `=== EventSource.CLOSED` дало бы undefined.
  if (aliveRef.current && (esRef.current === null || esRef.current.readyState === 2)) {
    if (timerRef.current) clearTimeout(timerRef.current); // гасим висящий reconnect-таймер из onerror (без двойного connect)
    connect(viewRef.current?.cursor ?? 0);
  }
};
globalThis.addEventListener("online", onOnline);
```

В cleanup (`return () => {…}` того же эффекта) добавить:
```ts
globalThis.removeEventListener("online", onOnline);
```

NB: `?? 0` — fallback курсора (не `?? since` как в спеке: `since` не виден в этом контексте эффекта; `viewRef` к моменту `online` уже заполнен стартовым GET, так что `0` недостижим). `timerRef`/`esRef`/`aliveRef`/`viewRef`/`connect` — существующие refs/функция хука.

- [ ] **Step 4: PASS (новый + существующие) + tsc**

Run: `cd frontend && npx vitest run src/game/useGame.test.tsx && npx tsc --noEmit`
Expected: все PASS, 0 ошибок

- [ ] **Step 5: Commit**

```bash
git add frontend/src/game/useGame.ts frontend/src/game/useGame.test.tsx frontend/src/test/eventsource.ts
git commit -m "feat(rj-puo): useGame переподключает SSE по событию online"
```

---

### Task 4: Бэк — MIME для sw/manifest + CSP manifest-src/worker-src

**Files:**
- Modify: `backend/app/app_factory.py` (spa-роут, ~строка 123-132)
- Modify: `backend/app/middleware/security_headers.py` (CSP)
- Modify: `backend/tests/api/test_spa_serving.py`
- Modify: `backend/tests/api/test_middleware.py`

**Interfaces:**
- Produces: корневые `.js`/`.webmanifest` отдаются с корректным `Content-Type`; CSP содержит `manifest-src 'self'` и `worker-src 'self'`.

- [ ] **Step 1: Тесты**

В `backend/tests/api/test_spa_serving.py` — расширить фикстуру `app_with_spa` реальными файлами и добавить кейсы. В фикстуре после создания `index.html`/`assets/app.js` добавить:
```python
    (dist / "sw.js").write_text("/* sw */", encoding="utf-8")
    (dist / "manifest.webmanifest").write_text('{"name":"Renju"}', encoding="utf-8")
```
Новые тесты:
```python
async def test_sw_js_mime(spa_client):
    r = await spa_client.get("/sw.js")
    assert r.status_code == 200
    assert "javascript" in r.headers["content-type"]
    assert "/* sw */" in r.text


async def test_manifest_mime(spa_client):
    r = await spa_client.get("/manifest.webmanifest")
    assert r.status_code == 200
    assert r.headers["content-type"] == "application/manifest+json"
```

В `backend/tests/api/test_middleware.py::test_security_headers` добавить:
```python
    assert "manifest-src 'self'" in r.headers["Content-Security-Policy"]
    assert "worker-src 'self'" in r.headers["Content-Security-Policy"]
```

- [ ] **Step 2: FAIL**

Run: `cd backend && uv run pytest tests/api/test_spa_serving.py tests/api/test_middleware.py -q`
Expected: CSP-кейсы (manifest-src/worker-src) FAIL точно. MIME-кейсы (`test_sw_js_mime`/`test_manifest_mime`) на dev-маке могут СРАЗУ пройти — системный Python `mimetypes` уже знает `.webmanifest`/`.js`. Это не значит «фикс не нужен»: на Ubuntu-проде `/etc/mime.types` может отдать `octet-stream` для `.webmanifest` → явный `media_type` снимает зависимость от хоста (см. спеку §«Стык с бэком»). TDD-«красное» по MIME тут не гарантировано — это ожидаемо.

- [ ] **Step 3: Реализация — MIME в `app_factory.py`**

В spa-роуте заменить корневую отдачу файла на маппинг media_type по суффиксу:

```python
        _MEDIA = {".webmanifest": "application/manifest+json", ".js": "text/javascript"}

        @app.get("/{full_path:path}")
        async def spa(full_path: str):
            if full_path.startswith("api/"):
                return JSONResponse({"detail": "Not Found"}, status_code=404)
            candidate = (dist / full_path).resolve()
            if full_path and candidate.is_file() and candidate.is_relative_to(dist_root):
                return FileResponse(candidate, media_type=_MEDIA.get(candidate.suffix))
            return FileResponse(dist / "index.html")
```

(`media_type=None` для прочих суффиксов → FileResponse сам определит, как раньше.)

- [ ] **Step 4: Реализация — CSP**

В `backend/app/middleware/security_headers.py` дополнить строку CSP директивами `manifest-src 'self'; worker-src 'self'`. Добавить их после `connect-src 'self';`:
```python
        "base-uri 'none'; frame-ancestors 'none'; connect-src 'self'; "
        "manifest-src 'self'; worker-src 'self'; "
```

- [ ] **Step 5: PASS + ruff**

Run: `cd backend && uv run pytest tests/api/test_spa_serving.py tests/api/test_middleware.py -q && uv run ruff check app tests`
Expected: PASS, no errors

- [ ] **Step 6: Commit**

```bash
git add backend/app/app_factory.py backend/app/middleware/security_headers.py backend/tests/api/test_spa_serving.py backend/tests/api/test_middleware.py
git commit -m "feat(rj-puo): MIME для sw/manifest + manifest-src/worker-src в CSP"
```

---

### Task 5: Иконки из лого + теги в `index.html`

**Files:**
- Create: `frontend/public/pwa-192.png`, `pwa-512.png`, `pwa-512-maskable.png`, `apple-touch-icon-180.png`, `favicon.ico`
- Modify: `frontend/index.html`

**Interfaces:**
- Produces: иконки в `public/` (Vite копирует в корень `dist`); `<link rel="apple-touch-icon">`, `<link rel="icon">`, `<meta name="theme-color">` в `index.html`.

- [ ] **Step 1: Сгенерировать иконки из `~/Downloads/image.png`**

```bash
cd /Users/alexey/code/Renju/frontend && mkdir -p public
SRC=~/Downloads/image.png
[ -f "$SRC" ] || { echo "НЕТ исходника $SRC — спросить Alexey"; exit 1; }
sips -z 192 192 "$SRC" --out public/pwa-192.png
sips -z 512 512 "$SRC" --out public/pwa-512.png
sips -z 512 512 "$SRC" --out public/pwa-512-maskable.png
sips -z 180 180 "$SRC" --out public/apple-touch-icon-180.png
sips -z 64 64 "$SRC" --out public/favicon-64.png && sips -s format microsoft-icon public/favicon-64.png --out public/favicon.ico && rm public/favicon-64.png
```
NB: maskable = та же картинка; safe-zone не вычищаем (лого уже с полями на тёмном фоне). Если на Android иконка обрежется некрасиво — отдельно сделать вариант с паддингом (вне этой задачи).

- [ ] **Step 2: Проверить, что иконки создались**

Run: `ls -la frontend/public/`
Expected: 5 файлов (pwa-192, pwa-512, pwa-512-maskable, apple-touch-icon-180, favicon.ico)

- [ ] **Step 3: Теги в `index.html`**

В `frontend/index.html` в `<head>` (после `<meta name="viewport" …>`) добавить:
```html
    <link rel="apple-touch-icon" href="/apple-touch-icon-180.png" />
    <link rel="icon" href="/favicon.ico" sizes="any" />
    <meta name="theme-color" content="#1c1a17" />
```

- [ ] **Step 4: tsc (sanity, теги HTML не ломают сборку типов)**

Run: `cd frontend && npx tsc --noEmit`
Expected: 0 ошибок

- [ ] **Step 5: Commit**

```bash
git add frontend/public/ frontend/index.html
git commit -m "feat(rj-puo): иконки PWA из лого + apple-touch/theme-color в index.html"
```

---

### Task 6: vite-plugin-pwa + регистрация SW

**Files:**
- Modify: `frontend/package.json` (devDep)
- Modify: `frontend/vite.config.ts`
- Create: `frontend/src/pwa.ts`
- Create: `frontend/src/pwa.test.ts`
- Modify: `frontend/src/main.tsx`
- Modify: `frontend/tsconfig.json` (types)

**Interfaces:**
- Consumes: иконки/теги (Task 5)
- Produces: SW + манифест на сборке; `registerPwa()` зарегистрирован.

- [ ] **Step 1: Установить плагин**

```bash
cd frontend && npm install -D vite-plugin-pwa
```

- [ ] **Step 2: tsconfig types**

В `frontend/tsconfig.json` `compilerOptions.types` добавить `"vite-plugin-pwa/client"`:
```json
    "types": ["vite/client", "vitest/globals", "@testing-library/jest-dom", "vite-plugin-pwa/client"]
```

- [ ] **Step 3: pwa.ts + тест**

```ts
// frontend/src/pwa.ts
import { registerSW } from "virtual:pwa-register";

export function registerPwa(): void {
  registerSW({ immediate: true });
}
```

```ts
// frontend/src/pwa.test.ts
import { it, expect, vi } from "vitest";

const registerSW = vi.fn();
vi.mock("virtual:pwa-register", () => ({ registerSW }));

it("registerPwa вызывает registerSW immediate", async () => {
  const { registerPwa } = await import("./pwa");
  registerPwa();
  expect(registerSW).toHaveBeenCalledWith({ immediate: true });
});
```

- [ ] **Step 4: Тест pwa.ts — PASS**

Run: `cd frontend && npx vitest run src/pwa.test.ts`
Expected: PASS

- [ ] **Step 5: VitePWA в `vite.config.ts`**

Добавить импорт `import { VitePWA } from "vite-plugin-pwa";` и в массив `plugins` (после `react()`):

```ts
    VitePWA({
      registerType: "autoUpdate",
      injectRegister: null,
      manifest: {
        id: "/",
        name: "連珠 · Renju",
        short_name: "Renju",
        description: "Профессиональное рэндзю против движка Rapfi",
        lang: "ru",
        theme_color: "#1c1a17",
        background_color: "#f4ecda",
        display: "standalone",
        start_url: "/",
        icons: [
          { src: "pwa-192.png", sizes: "192x192", type: "image/png" },
          { src: "pwa-512.png", sizes: "512x512", type: "image/png" },
          { src: "pwa-512-maskable.png", sizes: "512x512", type: "image/png", purpose: "maskable" },
        ],
      },
      workbox: {
        globPatterns: ["**/*.{js,css,html,svg,png,ico,webmanifest,webp}"],
        navigateFallback: "/index.html",
        navigateFallbackDenylist: [/^\/api\//],
      },
    }),
```

- [ ] **Step 6: registerPwa в main.tsx**

В `frontend/src/main.tsx` добавить `import { registerPwa } from "./pwa";` и вызвать `registerPwa();` перед/после `createRoot(...).render(...)`.

- [ ] **Step 7: tsc + сборка + проверка CSP-чистоты index.html**

```bash
cd frontend && npx tsc --noEmit && npx vite build
# инлайн-скрипт = <script> БЕЗ src (его и режет CSP script-src без 'unsafe-inline').
# grep "<script>" бесполезен: реальные теги Vite всегда с атрибутами (<script type="module" src=…>).
grep -nE '<script(\s[^>]*)?>[^<]' dist/index.html && echo "ЕСТЬ inline-script — CSP сломает" || echo "inline-script нет — ок"
ls dist/sw.js dist/manifest.webmanifest       # сгенерированы
```
Expected: tsc 0 ошибок; build успешен; «inline-script нет — ок»; `sw.js`+`manifest.webmanifest` на месте.

- [ ] **Step 8: Полный прогон фронта**

Run: `cd frontend && npx vitest run`
Expected: все тесты зелёные

- [ ] **Step 9: Commit**

```bash
git add frontend/package.json frontend/package-lock.json frontend/vite.config.ts frontend/src/pwa.ts frontend/src/pwa.test.ts frontend/src/main.tsx frontend/tsconfig.json
git commit -m "feat(rj-puo): vite-plugin-pwa — манифест, SW, CSP-совместимая регистрация"
```

---

## Ручная проверка (Alexey, после мержа+деплоя)

- Установка на телефон (Android: «Установить»; iOS Safari: «На экран Домой» — иконка из лого).
- Запуск из иконки — standalone (без адресной строки).
- Выключить сеть на устройстве → баннер «Нет связи»; вернуть сеть → партия/списки продолжают работать (reconnect).
- Консоль чистая (нет CSP-ошибок про manifest/sw/beacon).
